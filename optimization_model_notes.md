# FracAI 布局优化模型说明

## 1. 建模对象

设备集合包括井口、管汇、压裂泵车、混砂车、砂罐、水罐、化添撬、仪表控制车、电源机组、消防应急区、道路和禁布区。每个设备以带尺寸的矩形建模，连接关系以有向管线图表达。

## 2. 约束分层

- 硬约束：设备不得碰撞，不得越界，不得占用禁布区。
- 强惩罚约束：管线穿越禁布区、需要道路的设备离道路过远、人员/仪表区进入高压核心区。
- 推荐约束：管线短、交叉少、转弯少；砂水化添靠近混砂车；混砂车、泵车、管汇、井口按流程方向布置。
- 可配置经验值：设备间最小/最大距离在原型中作为评分默认值，不作为标准距离宣称。

## 3. 优化目标

综合评分由可解释子项构成：

- 安全距离最大化：低于当前规则距离则扣分并给出具体设备对。
- 设备碰撞率为 0：碰撞属于硬违规。
- 禁布区避让：设备重叠和管线穿越均扣分。
- 管线总长最小化：连接图总长度越短越好。
- 车辆通行顺畅：需道路设备到道路距离不超过原型服务半径。
- 流程路径合理：砂/水/化添、混砂、压裂泵、管汇、井口按工艺顺序组织。
- 空间利用提升：接近不同规模井场目标占地率时得分较高。

## 4. 算法流程

1. 按井场长宽比、规模和模板适配度选择多个初始模板。
2. 建立固定连接图：砂/水/化添 -> 混砂车，混砂车/压裂泵车 -> 管汇，管汇 -> 井口，电源 -> 仪表车。
3. 对每个模板生成多个随机扰动初值。
4. 使用规则修复器先处理越界、禁布区和碰撞。
5. 使用模拟退火式局部搜索移动可动设备，接受更优解，少量接受近似劣解以跳出局部最优。
6. 使用局部微调对高风险设备做小步修复，降低硬违规和主要告警。
7. 输出多个候选方案，排序时优先比较硬约束风险，再比较告警数量和权重画像下的综合得分。

本算法定位为竞赛原型中的可解释启发式优化，不宣称替代施工级工程设计审查。原型中的设备间默认距离用于演示评分和风险提示，正式部署时应由企业 HSE 标准、项目设计要求和现场审查规则替换或确认。

## 5. 权重画像

系统支持综合均衡、安全优先、效率优先、成本优先、施工便捷、占地最小六种画像。权重只改变排序和综合分，不会取消硬约束。

## 6. 环境地势适配

系统新增平整平台、单向坡地、沟谷地势、台脊地势、水敏/环保区五类环境情景。三维场景按同一高程函数生成高程色带和等高线图；优化算法同步使用该环境参数调整模板选择、避让区和移动搜索方向。

- 平整平台：优先紧凑布置和缩短管线。
- 单向坡地：高压核心避开低洼侧，设备倾向沿等高线展开。
- 沟谷地势：中部汇水廊道作为避让带，设备分列在两侧台地。
- 台脊地势：井口和管汇优先靠近稳定高台，物料与保障设备布置在外缘。
- 水敏/环保区：水罐、化添撬、混砂车和管线远离低洼敏感区，强化泄漏与汇流风险控制。

综合评分新增“环境适配”指标，并在迭代轨迹、评分解释和导出 JSON/报告摘要中保留对应证据。

## 7. 后续可扩展

- 将设备间距离表改为企业 HSE 标准库。
- 接入真实井场 CAD/GIS 边界、多边形禁布区和道路宽度。
- 引入 NSGA-II 或混合整数规划，输出 Pareto 前沿而非单一加权排序。
- 增加高压管线转弯数、道路转弯半径、吊装作业面和消防车回转半径校核。
- 增加多井口、拉链式压裂和电驱压裂橇组场景。
- 将 Three.js 三维场景与真实设备 CAD/GLB 模型库对接，支持评审演示和工程复核两种精度。
